//custom api
//https://peaceful-springs-91970.herokuapp.com/api/
let data = null
let results = document.querySelector("#results")

function cardRepeat(info, element){
    let snippet = '';
    // Example of the snipped is in the html
    element.innerHTML+= snippet
}


document.addEventListener('DOMContentLoaded',async function(e) {


    e.preventDefault()

    // Set the results target to an empty div
    

    // Insert GET function that pulls from https://peaceful-springs-91970.herokuapp.com/api/
    
    // Convert the data to JSON
    
    // Loop through the data and call the cardRepeat function to populate the data (not, this one is built a bit differently than the last one)
    for (let i = data.length - 1; i >= 0; i--) {
        
    }

})